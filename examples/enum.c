#include "enum.h"

/* Seem to need one typedef'd enum to get them all in the TU. 
   Or have a typedef'd one first!
*/
PrimaryColor color;

//enum Foo ff;

#if 0
int x = Y;
enum Failure f;

int 
foo()
{
PrimaryColor color;
enum Failure f;
//enum wxStretch s = wxGROW;
}

#endif



