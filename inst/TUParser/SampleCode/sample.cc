#include <math.h>
#include <string.h>
int
main(int argc, char *argv[])
{

    int x, y;
    int val;
    float a, b;
    val = x | y;
    val = x^y;
    x = abs(x);
    a = abs(a);
    x++;
    x--;
    a = a + b;
}

